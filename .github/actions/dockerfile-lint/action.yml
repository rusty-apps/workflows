name: "Dockerfile Lint Action"
description: "Action that runs squ1d123/dockerfilelint linting tool"
author: "Tim Preece"
inputs:
  dockerfile:
    required: false
    description: "The path to the Dockerfile to lint"
    default: "Dockerfile"
runs:
  using: "docker"
  image: "mrdoodles/dockerfilelint:1.0.0"
  args:
    - ${{ inputs.dockerfile }}
branding:
  icon: "aperture"
  color: "red"
